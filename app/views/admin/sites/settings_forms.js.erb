for(var item of document.querySelectorAll(".pz-tab-link")) { 
  item.classList.remove('pz-active') 
}
document.getElementById('settings-form-<%= params[:settings_form] %>').classList.add('pz-active')
document.getElementById('settings-forms-con').innerHTML = '<%= j render partial: "#{params[:settings_form]}_settings_form", format: :html  %>'

var inputSource = document.querySelector('#site_blog_attributes_name');
var inputTarget = document.querySelector('#site_blog_attributes_permalink');

if(inputSource != null && inputTarget != null) {
  window.Pz.slugify(inputSource, inputTarget);
}
