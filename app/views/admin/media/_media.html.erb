<% @all_media.each do |media| %>
  <!-- card -->
  <div class="nature-card" data-tags="a beautiful landscape - nature outdoor">
    <div class="uk-card uk-card-small uk-card-default">
      <div class="uk-card-media">
        <div class="uk-inline-clip uk-transition-toggle" tabindex="0">
          <% if media.attachment.attached? %>
            <%= image_tag media.attachment, class: "lazy" if media.attachment.attached? %>
          <% end %>
          <%# <img class="lazy" data-src="https://picsum.photos/400/350/?random=1" data-width="400" data-height="350" data-uk-img alt="" src="img/transp.gif"> %>
          <div class="uk-transition-slide-bottom uk-position-bottom uk-overlay uk-overlay-primary">
            <%= link_to  admin_media_path(media), method: :delete, remote: true do %>
              <span data-uk-icon="icon:trash; ratio: 1.3"></span>
            <% end %>
            <span data-uk-icon="icon:pencil; ratio: 1.3"></span>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- /card -->
<% end %>
