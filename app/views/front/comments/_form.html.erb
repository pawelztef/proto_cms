<%= form_with model: [commentable, Comment.new], local: true, html: { class: local_assigns[:class] } do |form| %>
  <fieldset class="uk-fieldset">
    <div class="uk-margin">
      <%= form.text_area :body, placeholder: "Add a commnet", class: "uk-textarea" %>
    </div>
    <div class="uk-margin">
      <%= form.hidden_field :parrent_id, value: local_assigns[:parrent_id] %>
      <%= form.submit class: "uk-button uk-button-default uk-button-small" %>
    </div>
  </fieldset>
<% end %>
