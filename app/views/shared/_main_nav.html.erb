<nav class="uk-navbar">
  <div class="uk-navbar-flip uk-navbar-center uk-visible-large">
    <ul class="uk-navbar-nav">
      <% @roots_pages.each do |page| %>
        <li><%= link_to page.name, page %></li>
      <% end %>
    </ul>
  </div>
  <div class="uk-navbar-flip uk-hidden-large">
    <a href="#offcanvas" class="uk-navbar-toggle" data-uk-offcanvas=""></a>
  </div>
</nav>
<%# <%= render 'shared/submenu_pages', pages: @page.root.children %1> %>
