$(() => {
  document.getElementById('password_button').addEventListener('click', function(e) {
    e.preventDefault();
    e.stopPropagation();
    <%# let target = document.getElementById('user_password'); %>
    <%# target.value = x; %>
  });
});
